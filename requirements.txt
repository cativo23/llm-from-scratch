# Pinned to the versions this project was built and tested with (Python 3.14).
openai==2.41.0
python-dotenv==1.2.2
rich==15.0.0
prompt_toolkit==3.0.52

# RAG (scripts 03 & 04): local embeddings + vector math, no API needed.
fastembed==0.8.0
numpy==2.4.6

# Framework comparison (scripts 05 & 06).
langchain==1.3.4
langchain-core==1.4.0
langchain-openai==1.2.2
langgraph==1.2.4
